import { FilesDragDropDirective } from './files-drag-drop.directive';

describe('FilesDragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new FilesDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
